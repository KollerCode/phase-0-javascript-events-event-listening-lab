function addingEventListener() {
    
const input = document.getElementById('input');

function clickAlert() {
  alert('I was clicked and it felt so good!');
}

input.addEventListener('click', clickAlert);

}
